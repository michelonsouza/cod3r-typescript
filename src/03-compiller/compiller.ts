const chanel = 'Gaveta';
const subscribers = 610234;

// chanel = subscribers;
console.log(`Chanel = ${chanel}`);

// let name = 'pedro';

function sum(a: number, b: number): number {
  return a + b;
}
